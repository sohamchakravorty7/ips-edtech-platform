import { SectionWrapper, SectionHeader } from "@/components/shared/section-wrapper"
import { StaggerContainer, StaggerItem, FadeIn } from "@/components/shared/motion-wrapper"
import {
  QuoteIcon,
  StarIcon,
  TrendingUpIcon,
  BriefcaseIcon,
  MapPinIcon,
} from "lucide-react"
import Link from "next/link"
import { Button } from "@/components/ui/button"
import { ArrowRightIcon } from "lucide-react"

interface Testimonial {
  name: string
  role: string
  location: string
  program: string
  content: string
  avatar: string
  gradient: string
  placedAt: string
}

const testimonials: Testimonial[] = [
  {
    name: "Priya Sharma",
    role: "Software Engineer II",
    location: "Bengaluru",
    program: "Full-Stack Web Development",
    content:
      "I went from a non-tech background to a 18 LPA offer at a top product company in 7 months. The mentors didn't just teach code — they taught me how to think like an engineer and present myself with confidence.",
    avatar: "PS",
    gradient: "from-[#0f3d91] to-[#4338ca]",
    placedAt: "Placed at Google India",
  },
  {
    name: "Rahul Verma",
    role: "Machine Learning Engineer",
    location: "Hyderabad",
    program: "Data Science & AI",
    content:
      "The data science program was rigorous in the best way. Real datasets, industry mentors, and a placement team that genuinely cared. I received 3 offers and chose the one that matched my career goals perfectly.",
    avatar: "RV",
    gradient: "from-[#4338ca] to-[#0f3d91]",
    placedAt: "Placed at Microsoft India",
  },
  {
    name: "Ananya Iyer",
    role: "Product Designer",
    location: "Mumbai",
    program: "Product Design & UI/UX",
    content:
      "What set iPS apart was the portfolio-first approach. By graduation, I had 6 case studies and a design system that caught the attention of recruiters immediately. Best career decision I've ever made.",
    avatar: "AI",
    gradient: "from-[#f4b400] to-[#e08e00]",
    placedAt: "Placed at Razorpay",
  },
]

export function Testimonials() {
  return (
    <SectionWrapper>
      <SectionHeader
        eyebrow="Success Stories"
        title="Real People. Real Careers. Real Results."
        description="Thousands of students have transformed their careers with iPS. Here are just a few of their stories."
      />

      <StaggerContainer className="grid gap-6 md:grid-cols-3">
        {testimonials.map((testimonial) => (
          <StaggerItem key={testimonial.name} className="h-full">
            <figure className="group relative flex h-full flex-col overflow-hidden rounded-3xl border border-border bg-card p-7 shadow-premium transition-all duration-500 hover:-translate-y-2 hover:border-gold/30 hover:shadow-[0_1px_2px_rgb(11_28_63/0.04),0_20px_48px_-12px_rgb(244_180_0/0.18)]">
              {/* Top gradient bar */}
              <div
                aria-hidden="true"
                className={`absolute inset-x-0 top-0 h-1 bg-gradient-to-r ${testimonial.gradient}`}
              />

              <QuoteIcon
                className="absolute right-6 top-6 h-10 w-10 text-foreground/[0.05] transition-colors duration-500 group-hover:text-gold/20"
                aria-hidden="true"
              />

              {/* Rating */}
              <div className="flex items-center gap-1" aria-label="5 out of 5 stars">
                {Array.from({ length: 5 }).map((_, i) => (
                  <StarIcon key={i} className="h-4 w-4 fill-gold text-gold" aria-hidden="true" />
                ))}
              </div>

              <blockquote className="mt-4 flex-1">
                <p className="text-sm leading-relaxed text-muted-foreground">
                  &ldquo;{testimonial.content}&rdquo;
                </p>
              </blockquote>

              {/* Placement badge */}
              <div className="mt-5 flex items-center gap-2 rounded-xl border border-emerald-600/20 bg-emerald-50 px-3.5 py-2.5 dark:bg-emerald-950/40">
                <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-emerald-600/10 text-emerald-700 dark:text-emerald-400">
                  <BriefcaseIcon className="h-3.5 w-3.5" aria-hidden="true" />
                </span>
                <span className="text-xs font-bold text-emerald-700 dark:text-emerald-400">
                  {testimonial.placedAt}
                </span>
              </div>

              {/* Author */}
              <figcaption className="mt-6 flex items-center gap-3 border-t border-border pt-5">
                <div
                  className={`flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-gradient-to-br ${testimonial.gradient} text-sm font-bold text-white shadow-lg`}
                  aria-hidden="true"
                >
                  {testimonial.avatar}
                </div>
                <div className="min-w-0">
                  <p className="truncate text-sm font-bold">{testimonial.name}</p>
                  <p className="truncate text-xs text-muted-foreground">
                    {testimonial.role}
                  </p>
                  <p className="flex items-center gap-1 truncate text-[0.7rem] text-muted-foreground/80">
                    <MapPinIcon className="h-3 w-3 shrink-0" aria-hidden="true" />
                    {testimonial.location} · {testimonial.program}
                  </p>
                </div>
              </figcaption>
            </figure>
          </StaggerItem>
        ))}
      </StaggerContainer>

      {/* Bottom stat + CTA */}
      <FadeIn delay={0.2}>
        <div className="mt-14 flex flex-col items-center justify-between gap-6 rounded-3xl border border-gold/20 bg-gradient-to-r from-royal-soft via-gold-soft/40 to-royal-soft px-8 py-8 text-center sm:flex-row sm:text-left">
          <div className="flex items-center gap-4">
            <span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-royal text-white shadow-[0_8px_24px_-6px_rgb(15_61_145/0.5)]">
              <TrendingUpIcon className="h-6 w-6" aria-hidden="true" />
            </span>
            <p className="text-sm leading-relaxed text-foreground sm:text-base">
              <span className="font-bold">Average salary hike of 168%</span> reported
              by iPS graduates within 12 months of graduation.
            </p>
          </div>
          <Link href="/#contact" className="shrink-0">
            <Button variant="gold" size="lg" className="group gap-2 rounded-full">
              Start Your Journey
              <ArrowRightIcon
                className="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1"
                aria-hidden="true"
              />
            </Button>
          </Link>
        </div>
      </FadeIn>
    </SectionWrapper>
  )
}